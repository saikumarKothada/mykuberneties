apiVersion: v1
kind: Pod
metadata:
   name: my-pod
   label: 
      app: my-app
spec:
   containers:
      - name: my-container
        image: httpd
        ports: 
          - containerPort: 80
        